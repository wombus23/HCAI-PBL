from django.apps import AppConfig


class Project3Config(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "project3"
    verbose_name = "Active learning for learning to defer"
