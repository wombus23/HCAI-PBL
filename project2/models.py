# Project 2 keeps no state: the dataset is fixed, the models are refitted from it
# on first use and cached in memory, and every control lives in the query string.
# So there is nothing to store in the database.
